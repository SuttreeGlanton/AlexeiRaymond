---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

{tags.length > 0 && (
  <div class="tags" aria-label="Piece tags">
    {tags.map((tag) => <span class="tag">{tag}</span>)}
  </div>
)}
