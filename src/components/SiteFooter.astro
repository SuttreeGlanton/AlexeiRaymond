---
import { site } from '../lib/data';
import { withBase } from '../lib/paths';

const year = new Date().getFullYear();

const footerLinks = [
  { label: 'Cycles', href: withBase('') },
  { label: 'Selected', href: withBase('selected/') },
  { label: 'Chronology', href: withBase('chronology/') },
  { label: 'About', href: withBase('about/') },
  { label: 'Elsewhere', href: withBase('elsewhere/') }
];

/* Every social with a label and a link shows up here; the icon is chosen
   by label, with a neutral globe for networks that have no dedicated glyph. */
const socialLinks = site.socials.flatMap((social) => {
  if (typeof social === 'string') return [];

  const label = social.label ?? social.name ?? '';
  const href = social.url ?? social.href ?? '';
  const kind = label === 'X' ? 'x' : label === 'Ko-fi' ? 'kofi' : label === 'Instagram' ? 'instagram' : 'globe';

  return label && href ? [{ label, href, kind }] : [];
});
---

<footer class="site-footer">
  <div class="page-shell footer-inner">
    <span class="footer-mark">&copy; {year} {site.author}</span>

    <nav class="footer-socials" aria-label="Social links">
      {socialLinks.map((item) => (
        <a
          class="footer-social-link"
          href={item.href}
          target="_blank"
          rel="noopener noreferrer"
          aria-label={item.label === 'Ko-fi' ? `Support ${site.author} on Ko-fi` : `${site.author} on ${item.label}`}
          title={item.label}
        >
          {item.kind === 'x' ? (
            <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
              <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231 5.45-6.231Zm-1.161 17.52h1.833L7.084 4.126H5.117L17.083 19.77Z" />
            </svg>
          ) : item.kind === 'kofi' ? (
            <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
              <path d="M5 9.5h11v4.75a4.25 4.25 0 0 1-4.25 4.25h-2.5A4.25 4.25 0 0 1 5 14.25V9.5Z" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linejoin="round" />
              <path d="M16 10.75h1.5a2.63 2.63 0 0 1 0 5.25H16" fill="none" stroke="currentColor" stroke-width="1.7" />
              <path d="M8.5 3.25v2M12 3.25v2" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" />
            </svg>
          ) : item.kind === 'instagram' ? (
            <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
              <rect x="3" y="3" width="18" height="18" rx="5" fill="none" stroke="currentColor" stroke-width="1.7" />
              <circle cx="12" cy="12" r="4.25" fill="none" stroke="currentColor" stroke-width="1.7" />
              <circle cx="17.35" cy="6.65" r="1" />
            </svg>
          ) : (
            <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
              <circle cx="12" cy="12" r="8.5" fill="none" stroke="currentColor" stroke-width="1.7" />
              <path d="M3.5 12h17M12 3.5c2.6 2.3 3.9 5.2 3.9 8.5s-1.3 6.2-3.9 8.5c-2.6-2.3-3.9-5.2-3.9-8.5s1.3-6.2 3.9-8.5Z" fill="none" stroke="currentColor" stroke-width="1.7" />
            </svg>
          )}
        </a>
      ))}
    </nav>

    <nav class="footer-nav" aria-label="Footer navigation">
      {footerLinks.map((item) => (
        <a href={item.href}>{item.label}</a>
      ))}
      <a href="#">Back to top &uarr;</a>
    </nav>
  </div>
</footer>
