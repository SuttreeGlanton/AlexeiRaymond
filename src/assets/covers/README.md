# Covers

Add black-and-white cover images here, then reference them from `site-data.json`, for example:

```json
"cover": "covers/mythbreaking.jpg"
```

The site also works intentionally with empty `cover` values: it renders typographic placeholders instead of broken images.
